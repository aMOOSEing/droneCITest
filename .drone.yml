steps:
- name: run-tests
  # Tests must run in GLibc based container because `race` does not
  # play nice with Musl.
  # See https://github.com/golang/go/issues/14481 for details.
  image: golang:1.23.3
  commands:
  - go build
  - go test
